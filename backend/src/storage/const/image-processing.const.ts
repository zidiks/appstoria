export const IMAGE_PROCESSING = {
  /** Белый паддинг вокруг объекта, px */
  padding: 8,
  /** Пиксель считается белым, если все каналы не меньше этого значения */
  whiteChannelMin: 240,
  /** Доля белых пикселей по рамке, ниже которой фон считается небелым */
  whiteBorderRatio: 0.9,
  /** Ширина рамки для анализа фона, доля от меньшей стороны */
  borderRatio: 0.03,
  /** Чувствительность trim к отклонению от фона */
  trimThreshold: 10,
  /** Разброс яркости, ниже которого картинка считается однотонной (объекта нет) */
  uniformStdevMax: 1,
  /** Сторона квадрата, до которой ужимаем слишком большие изображения */
  maxSide: 2000,
  /** Размер уменьшенной копии, на которой анализируем фон */
  sampleSize: 128,
  background: '#ffffff',
  /**
   * Настройки webp. Дефолтные для sharp — quality 80: на них обработка добавляла
   * заметную вторую генерацию потерь поверх сжатия, сделанного при загрузке.
   * quality 95 + smartSubsample убирают её почти полностью (PSNR 38.6 -> 44.2 dB)
   * ценой примерно +45% размера. effort выше 4 не окупается: +0.2 dB за +57%
   * времени кодирования. Для абсолютной точности можно поставить lossless: true,
   * но файл вырастает кратно, а потери первой генерации это всё равно не вернёт.
   */
  webp: {
    quality: 95,
    smartSubsample: true,
  } as const,
};
